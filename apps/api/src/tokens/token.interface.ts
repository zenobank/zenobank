import { Network } from 'src/lib/contants/network';

// export interface Token {
//   id: string;

//   // share between same token in different networks
//   // USDC in base, arbitrum, etc. will have the same id
//   crossNetworkTokenId: string;

//   name: string;
//   symbol: string;
//   tokenAddress: string;
//   network: Network;
//   decimals: number;
// }
